/** @type {import('next').NextConfig} */
const nextConfig = {
  allowedDevOrigins: ['10.177.218.47'],
  // This completely disables the new floating DevTools UI in Next.js 15+
  devIndicators: false,
}

module.exports = nextConfig
